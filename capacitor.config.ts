import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nkentech.recipe',
  appName: 'ionic-angular-course-2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
